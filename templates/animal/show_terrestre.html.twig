{% extends 'base.html.twig' %}

{% block title %}Habitat aquatique{% endblock %}

{% block body %}


<div class="container-fluid">
    <div class="row bg-light justify-content-center py-4">
        {% if terrestre %}
                <img src="{{ asset('uploadsAnimals/imagesAnimals/' ~ terrestre.imagesAnimal) }}" height="500px" alt="Image de {{ terrestre.prenom }}">
                <div class="col-lg-12">
                    <div class="down-content">
                        <h4> {{ terrestre.prenom }}</h4>
                        <span>{{ terrestre.race }}</span>
                    </div>
                </div>
        {% endif %}
    </div>
</div>
{% endblock %}
