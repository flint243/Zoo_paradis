{% extends 'base.html.twig' %}

{% block title %}Zoo paradis | Habitat aquatique{% endblock %}

{% block body %}


<div class="container justify-content-center">
       {% if aquatique %}
    <div class="row py-4">
        <div class="col justify-content-around p-2">
            <button class="btn btn-success"><a href="/habitats/aquatique"> Retour </a></button>
            <button class="btn btn-success"><a href="/habitats/">Habitats </a></button>
        </div>
        <div class="col-12 bg-light text-center py-4">
            <h4> {{ aquatique.prenom }}</h4>
            <img src="{{ asset('uploadsAnimals/imagesAnimals/' ~ aquatique.imagesanimal) }}" class="singleImage" alt="Image de {{ aquatique.prenom }}">
                <div class="down-content">
                    <span>{{ aquatique.race }}</span>
                    <p>{{ clickCount }} <i class="fa fa-eye"></i></p>
                </div>            
        </div>
        </div>
            {% endif %}
</div>
{% endblock %}
